import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
	suggestionBox: { 
		position:'absolute',
		width: '100%',
		left: '0',
		padding: '10px',
	}
}));

export default useStyles;
